import { createSelector } from 'reselect';

// TODO а это уже нужно с помощью reselect где есть товары в сторе
// вероятно придется в корзину класть все данные по товару...
// selectSum: ({ items }) =>
//   items.reduce((sum, { quantity }) => sum + quantity, 0),

// TODO выбрать id всех продуктов в корзине
// TODO см есть ли конкретный продукт в корзине (с карточки)

export {};
